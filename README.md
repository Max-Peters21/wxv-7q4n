# wxv-7q4n

Personal phone PWA.

## Install

Open the GitHub Pages URL in iPhone Safari, then use Share > Add to Home Screen.

## Notes

The page asks search engines not to index it, and `robots.txt` blocks crawlers. This is still a public repository, so it is not private.
